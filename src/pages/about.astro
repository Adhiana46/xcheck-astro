---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "About Me";

const identity = {
  firstName: "Adhiana",
  lastName: "Mastur",
  country: "Indonesia",
  occupation: "Backend Engineer",
  hobbies: ["Coding", "Riding Bus", "Working"],
};

const skills = ["HTML", "CSS", "Javascript", "PHP"];

const happy = false;
const suffering = true;
const finished = false;
const goal = 3;

const colors = {
  skillColor: "navy",
  hobbyColor: "pink",
};

const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
  <p>
    I am working through Astro's introductory tutorial. This is the second page
    on my website, and it's the first one I built myself!
  </p>
  <p>
    This site will update as I complete more of the tutorial, so keep checking
    back and see how my journey is going!
  </p>
  <p>Here are a few facts about me:</p>
  <ul>
    <li>My name is {identity.firstName} {identity.lastName}.</li>
    <li>
      I live in {identity.country} and I work as a {identity.occupation}
    </li>
    <li>
      My Hobby are:
      <ul>
        {identity.hobbies.map((item) => <li class="hobby">{item}</li>)}
      </ul>
    </li>
  </ul>
  <p>My skills are:</p>
  <ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>

  {happy && <p>I am happy to be learning Astro!</p>}
  {suffering && <p>I am suffering to be learning Astro!</p>}
  {finished && <p>I finished this tutorial!</p>}
  {
    goal === 3 ? (
      <p>My goal is to finish in 3 days.</p>
    ) : (
      <p>My goal is not 3 days.</p>
    )
  }
</BaseLayout>

<style define:vars={{ ...colors, fontWeight, textCase }}>
  .skill {
    color: var(--skillColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }
  .hobby {
    color: var(--hobbyColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }
</style>
